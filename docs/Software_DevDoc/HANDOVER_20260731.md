# HANDOVER 2026-07-31 (S10 uzavřena — čeká na commit)

## Aktuální stav
Bench test je **kompletní s výjimkou T3/T4** (jediný zbylý blocker — fyzický
hardware, OI1, ne software). S8/S9 (ADR-010 topné kabely, ADR-011 umístění
čidel) už byly dřív commitnuté a pushnuté (`5b15105`). Dnešní S10 (ADR-004
led_sequencer + BUG-005/006) je zkompilovaná a bench potvrzená, čeká na
poslední kontrolu dokumentace a commit.

## Co je hotovo a bench-potvrzeno (celý dosavadní rozsah)
- **OI8 (ADR-006)**, **OI9 (ADR-007+008)**, **OI6 (ADR-009)** — defrost logika
  (boot-resume, floor/ceiling, HEAT_MODE gate). Bench potvrzeno dřív.
- **ADR-010** — samoregulační topný kabel (odtok 10 W/m/7m, chassis 20 W/m/~2,5m).
- **ADR-011** — umístění čidel T2 (plynový vývod výměníku)/T3 (mezera meandru
  chassis kabelu), rename `t_evap`→`t_gas_inlet`.
- **OI10** — `led_sequencer` custom komponenta portována z Garage_Windows.
- **ADR-004** — `led_sequencer` naportován do produkčního YAML: ERR LED (5 kódů,
  `err_wifi`/`err_t1..t4`), WD LED (4 vzájemně se vylučující stavy). **Bench
  potvrzeno** — WD kompletně, ERR `err_wifi`/`err_t1`/`err_t2` + sekvenční
  přehrávání více chyb. `err_t3`/`err_t4` zbývají (OI1).
- **OI11** — `err_t1..t4` split, detekce čte `_used` vrstvu místo raw senzoru.
- **BUG-001 až BUG-006** — všechny opravené a bench potvrzené (viz `BUGS.md`).
  BUG-005/006 nalezeny a opraveny dnes (S10): sim_mode race (recidiva BUG-002,
  jiný root cause) a pomalá detekce selhání senzoru (median filtr maskoval
  NaN, teď zakomentován, `update_interval` 120s→20s/5s).
- **OI12, OI18** — supersedovány BUG-006 fixem, hotovo.

## Co zbývá — jen T3/T4 (OI1)
- **Fáze 4** (`TEST_PLAN.md`) — T3/T4 warm-up window po komisioningu
- **Fáze 7** (`TEST_PLAN.md`) — `err_t3`/`err_t4` pulzní kódy (4×/5×)
- Obojí čeká na fyzické DS18B20 senzory pro T3/T4 — až budou k dispozici,
  postup stejný jako u T1/T2 (S5): `test01.yaml` bring-up, přečíst adresy z
  bootlogu, zapsat do produkčního YAML + `ARCHITECTURE.md`.

## Fronta (po OI1)
1. OI1 → dokončit Fázi 4 a Fázi 7 (T3/T4)
2. Entity rename session (ADR-005) — sjednotí odložené renamy z ADR-007/008/009
3. B-VALID-01/02/03 — vyhodnotit po 1. zimní sezóně
4. Fyzická montáž čidel dle ADR-011 (T2 na trubku b, T3 v mezeře meandru)
5. Fyzická montáž topných kabelů dle ADR-010

## Revision header (freshness anchor)
SESSION_LOG poslední: S10 — 2026-07-31 (Implementer/CC), samostatná session,
uzavřena. S9/S8 (Architekt, 2026-07-31/30) pod ní, oba vnořené v S5
(2026-07-21 až 2026-07-30, dokončeno).
