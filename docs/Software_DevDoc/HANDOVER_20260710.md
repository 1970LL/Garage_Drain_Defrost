# HANDOVER 2026-07-10 (po S2 — Architekt)

## Aktuální stav
Fáze 1 (Doc-sync & Code Review). S2 byla architektonická session nad
improvement body (konzistence s Garage_Windows). Rozhodnuto, čeká na commit
+ na dvě navazující implementační session.

## Rozhodnutí S2 (k commitu přes CC)
- ADR-003 — sync-first rituál; revision header = poslední ## SN v SESSION_LOG
  (ROADMAP patička se pro freshness NEpoužívá — ve Windows driftovala).
- ADR-004 — port led_sequencer (Windows ADR-010) + DD pattern sada.
  ERR = 5 separátních kódů (wifi/T1/T2/T3/T4). WD = coarse, 1 aktivní stav.
- ADR-005 — HA-boundary princip: ESP derivuje vše, HA jen zobrazuje.
  CZ názvy, grouping přes device_id, ESP-side "Stav systému" + "Kód chyby".

## Fronta session (v pořadí)
1. CC doc-commit (prompt níže) — zapíše ADR-003/004/005 + BACKLOG + README fix.
2. Implementační session (Implementer/CC) — led_sequencer port, ERR detekce
   split na err_t1..t4, WD výběrová lambda, uptime senzor. = "code review"
   dle ROADMAP DoD Fáze 1, rozšířené o ADR-004.
3. Entity rename + grouping session (ADR-005 execution) — po impl. session
   (nové err_t1..t4 entity chtějí CZ názvy).

## Otevřené v BACKLOG
- OI5 run_time: ESP uptime parita (raw sec diagnostic + d/h/m HA-side).
  Runtime topení (nasčítaný čas) = nice-to-have, nad Windows scope.
- OI6 heat_mode decoupling: DEFROST_ORDERED dnes ignoruje HEAT_MODE
  (kontroluje jen main_system_enabled) — záměr? K prověření v code review.
- Předběžné OI1–OI4 z ARCHITECTURE §7 stále platí.

## Revision header (freshness anchor)
SESSION_LOG poslední: S2 — 2026-07-10.
