# HANDOVER 2026-07-28 (uprostřed S5 — bench test přerušen)

## Aktuální stav
Fáze 1 (Doc-sync & Code Review), přechod k reálnému bench testu na hotovém HW.
S5 (Implementer/CC) pokračuje od 2026-07-21 — implementoval ADR-006/007/008,
opravil prostředí (esptool), ověřil hardware (`test01.yaml`), komisionoval T1/T2,
a rozjel bench test dle `TEST_PLAN.md`. **Test přerušen 2026-07-28 po dokončení
Fáze 0–2** — Lubor pokračuje příště.

## Co je hotovo a bench-potvrzeno
- **OI8 (ADR-006)** — boot-resume přes edge-trigger. Bench potvrzeno (Fáze 2).
- **BUG-001** — template switche (`sw_main_system_enable`, `sim_mode`) resetovaly
  `restore_value: true` globály na každém bootu (default `restore_mode: ALWAYS_OFF`).
  Opraveno (`restore_mode: DISABLED`), bench potvrzeno.
- **BUG-002** — přepnutí `sim_mode` vždy spustilo oba heatery (race condition mezi
  4 nezávisle pollovanými `_used` senzory). Opraveno (`component.update` synchronně
  po přepnutí), bench potvrzeno.
- **OI1 částečně** — T1/T2 reálné DS18B20 adresy komisionovány (test01 bring-up).
  T3/T4 zůstávají placeholder.
- Hardware (finished PCB) ověřen plně funkční přes `test01.yaml` (všechny DI/DO,
  rezervy, WD/ERR LED).

## Co zbývá v bench testu (TEST_PLAN.md)
- **Fáze 3 (OI9/ADR-007+008)** — floor + ceiling scénáře 3a–3d (floor garance,
  pokles podmínky, ceiling cutoff, retrigger). Zatím netestováno.
- **Fáze 4 (OI12)** — T1/T2 část teď testovatelná (reálné adresy hotové), T3/T4
  zůstává blokováno na OI1.
- **Fáze 5** — regrese sousedních systémů (Main Switch, HEAT_MODE, error detekce, LED).
- **Fáze 6** — reálný výstup se zkušební zátěží, teprve pak reálné topné kabely.

## ⚠️ Nic není commitnuté
Celý rozsah S5 (ADR-008 implementace, BUG-001+002 fixy, T1/T2 adresy, test01.yaml)
čeká na dokončení bench testu, pak jednorázový commit + push. Firmware je aktuálně
zkompilovaný a flashnutý na desce s oběma bugfixy — pokud se v mezičase HW/firmware
nezmění, lze pokračovat rovnou od Fáze 3.

## Fronta (po dokončení bench testu)
1. Dokončit Fáze 3–6 `TEST_PLAN.md`
2. Commit + push celého S5 rozsahu
3. Implementační session pro ADR-004 (led_sequencer port — vyžaduje OI10,
   zkopírovat custom_components z Windows), ERR split, WD lambda, uptime
4. Entity rename session (ADR-005) — sjednotí odložené renamy z ADR-007/008

## Revision header (freshness anchor)
SESSION_LOG poslední: S5 — 2026-07-21 až 2026-07-28 (tento).
