# HANDOVER 2026-08-01 (S11 uzavřena — čeká na commit)

## Aktuální stav
**Bench test (S5–S11) je kompletně uzavřen — žádný zbylý blocker.** OI1
(T3/T4 fyzické senzory) vyřešeno: T3=`0xa90625910004ba28`,
T4=`0xb6062591abac6f28` (test01 HW bring-up). TEST_PLAN.md Fáze 4 a Fáze 7
dokončeny na reálném HW, vč. `err_t3`/`err_t4` (4×/5× pulz).

## Co je hotovo a bench-potvrzeno (celý dosavadní rozsah)
- **OI8 (ADR-006)**, **OI9 (ADR-007+008)**, **OI6 (ADR-009)** — defrost logika
  (boot-resume, floor/ceiling, HEAT_MODE gate).
- **ADR-010** — samoregulační topný kabel (odtok 10 W/m/7m, chassis 20 W/m/~2,5m).
- **ADR-011** — umístění čidel T2 (plynový vývod výměníku)/T3 (mezera meandru
  chassis kabelu), rename `t_evap`→`t_gas_inlet`.
- **OI10** — `led_sequencer` custom komponenta portována z Garage_Windows.
- **ADR-004** — `led_sequencer` naportován: ERR LED (5 kódů, všech 5 potvrzeno
  vč. `err_t3`/`err_t4`), WD LED (4 vzájemně se vylučující stavy). **Bench
  potvrzeno kompletně.**
- **OI11** — `err_t1..t4` split, detekce čte `_used` vrstvu.
- **BUG-001 až BUG-006** — všechny opravené a bench potvrzené (viz `BUGS.md`).
- **OI12, OI18** — supersedovány BUG-006 fixem.
- **OI1** — T1-T4 reálné adresy komisionovány (T1/T2 S5, T3/T4 S11). **Hotovo.**

## Co zbývá — žádný software blocker
Bench test firmwaru je funkčně kompletní. Zbylá práce je BACKLOG cleanup +
jedna větší odložená session (ADR-005 execution), viz Fronta níže.

## Fronta (dohodnuté pořadí, S11, 2026-08-01)
1. **OI17 + OI4** — event-driven refresh `_used` vrstvy (`on_value:` na raw
   senzorech + na `sim_t_*` number entitách místo plošného `update_interval:
   5s`), zvážit zároveň zkrácení 60s error-check intervalu. Řešení odsouhlaseno
   s Luborem, "mirror raw polling schedule" varianta zamítnuta (staleness
   riziko, zpomalila by Simulation Mode).
2. **OI13 + OI14** — drobný úklid (text_sensor 60s lag, `wifi.ap` password).
3. **ADR-005 execution** — entity rename (CZ friendly names), device_id
   grouping, ESP-side "Stav systému"/"Kód chyby" text senzory. Samostatná
   session, čeká od S2.
4. **OI7** — web_server auth/removal, před Field Deployment (ne teď).
5. **Odloženo na zimu:** OI15/16/19, B-VALID-01/02/03 — potřebují reálná
   zimní data.
6. **Mimo software:** fyzická montáž topných kabelů (ADR-010) a čidel T2/T3
   (ADR-011).

## Revision header (freshness anchor)
SESSION_LOG poslední: S11 — 2026-08-01 (Implementer/CC), uzavřena. S10
(2026-07-31) pod ní. S9/S8 (Architekt) vnořeny v S5 (2026-07-21 až 2026-07-30).
