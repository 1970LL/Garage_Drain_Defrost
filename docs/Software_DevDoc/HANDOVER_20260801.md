# HANDOVER 2026-08-01 (S15 uzavřena)

## Aktuální stav
**Bench-validace (Fáze 0–10, `TEST_PLAN.md`) je kompletně uzavřena.** HA
napárováno a importovalo se čistě (S15). Softwarová práce pro `ROADMAP.md`
Fáze 1–3 je hotová — projekt je ve **Fázi 4 (Field Deployment)**. Žádná
aktivní BACKLOG položka je teď akcionovatelná (všechny čekají na fyzickou
montáž nebo na zimní pozorování).

## Co je hotovo a bench-potvrzeno (celý dosavadní rozsah, S1–S15)
- **OI8 (ADR-006)**, **OI9 (ADR-007+008)**, **OI6 (ADR-009)** — defrost logika
  (boot-resume, floor/ceiling, HEAT_MODE gate).
- **ADR-010** — samoregulační topný kabel (odtok 10 W/m/7m, chassis 20 W/m/~2,5m).
- **ADR-011** — umístění čidel T2 (plynový vývod výměníku)/T3 (mezera meandru
  chassis kabelu), rename `t_evap`→`t_gas_inlet`.
- **OI10 + ADR-004** — `led_sequencer` naportován z Garage_Windows: ERR LED
  (5 kódů), WD LED (4 stavy). Bench potvrzeno kompletně.
- **OI11** — `err_t1..t4` split, detekce čte `_used` vrstvu.
- **OI1** — T1-T4 reálné adresy komisionovány (T1/T2 S5, T3/T4 S11).
- **OI12, OI18** — supersedovány BUG-006 fixem.
- **OI17** — `_used` senzory event-driven refresh (`update_interval: never` +
  `on_value:`/`on_boot:`), nahradilo plošný 5s poll.
- **OI4** — T3/T4 error-check interval 60s→20s (konzistence s T1/T2).
- **OI13** — "Kód chyby" text_sensor event-driven refresh (`on_state:` na
  error binary_sensorech), nahradilo plošný 60s poll.
- **OI14** — `wifi.ap` password, `ota:` list syntax.
- **OI5** — ESP uptime parita ("Čas chodu ESP32 (s)"); heater-runtime část
  vydělena jako OI20 (odloženo, nice-to-have).
- **ADR-005 execution (S14+S15)** — CZ friendly names na všech ~38 entitách,
  `esphome: devices:` grouping (Globální/Provoz/Nastavení/Servisní), nová
  entita "Stav systému", `icon:` na všech 35 exponovaných entitách. Draft
  archivováno: `#Archive/ADR-005_execution_draft.md`.
- **BUG-001 až BUG-007** — všechny opravené a bench potvrzené (viz `BUGS.md`).
- **AP-001** — zdokumentováno (tvrdý HW reset vs. `flash_write_interval`),
  systémové chování, ponecháno beze změny.
- **Dokumentační úklid (S15)** — README.md, `ROADMAP.md`, `PROJECT_VISION.md`,
  `Software_DevDoc_structure.md`, `ARCHITECTURE.md`, `BACKLOG.md`, `TEST_PLAN.md`
  kříženě zkontrolovány a stale reference opraveny (fázový stav, staré názvy
  entit T2, stará popis heater timingu). Nový návrh **ADR-005+** (obecná
  doporučení pro ikony/grouping + lessons learned z HA-prep procesu) čeká na
  Architekta: `docs/Software_DevDoc/ADR-005-plus_proposal_draft.md`.

## Co zbývá — žádný software blocker
Vše aktivní v `BACKLOG.md` je vázané na:
- **Field Deployment** (OI7 — web_server auth/removal, ne teď)
- **Zimní pozorování** (OI15/16/19/20, B-VALID-01/02/03 — potřebují reálná data)
- **Fyzickou montáž** (mimo software) — topné kabely (ADR-010), finální
  umístění čidel T2/T3 (ADR-011)

## Fronta (další session, až bude co dělat)
1. **Fyzická montáž** topných kabelů + přemístění T2/T3 — na Luborovi, mimo
   software.
2. **ADR-005+ návrh** — čeká na Architekta k formalizaci (viz výše).
3. **Po první zimní sezóně:** B-VALID-01/02/03, OI16, OI19 — vyhodnotit dle
   reálných dat.
4. **Field Deployment start:** OI7 (web_server).

## Revision header (freshness anchor)
SESSION_LOG poslední: S15 — 2026-08-01 (Implementer/CC), uzavřena. S14 pod ní
(ADR-005 execution). S13/S12/S11 pod nimi (OI13/14/5, OI17/4+AP-001, OI1
T3/T4). S9/S8 (Architekt) vnořeny v S5 (2026-07-21 až 2026-07-30).
